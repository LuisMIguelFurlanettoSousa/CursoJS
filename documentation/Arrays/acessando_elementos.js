const array = ['Este é o primeiro elemento', 'este é o segundo']

console.log(array[array.length - 1]);
console.log(array[0]);

const renderer = []

console.log(renderer['3d'].setTexture(model, 'personagem.png'))